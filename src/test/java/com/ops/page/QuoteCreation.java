package com.ops.page;

import com.base.BasePage;
import org.openqa.selenium.WebDriver;

public class QuoteCreation extends BasePage{
    public QuoteCreation(WebDriver driver) {
        super(driver);
    }
    // Add page-specific methods here
}